import { Typography } from '@mui/material';
import React from 'react';

const Banner = () => {
     return (
          <div>
               <Typography>this is banner part</Typography>
          </div>
     );
};

export default Banner;